exports.hidden = true;
exports.invoke = function (shell) {
    shell.log("Example 3: This shell has two commands. The first allows you to create cookies that are stored on the client. The second allows you to view all stored cookies.");
    shell.log();
    shell.log("Type \"help\" to see the available commands.");
};
