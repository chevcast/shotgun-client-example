exports.hidden = true;
exports.invoke = function (options, shell) {
    shell.log("Example 1: This shell includes example command modules \"topic\" and \"login\".");
    shell.log("Type \"help\" to see the available commands.");
};
